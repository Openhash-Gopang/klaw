[면책 문구] 이 문서에 수록된 모든 데이터는 자체 실험 결과이며 제3자의 검증을 거치지 않았습니다. 또한 일부는 초안 상태입니다. 따라서 이 문서의 어떠한 내용도 어떠한 용도로도 인용하거나 이용해서는 안 됩니다.

K-Law 방법론 v15.1 평가 프로토콜 정합 패치안 (2026. 9. 24., 적용 보류)
대상: klaw_v15_1.md (기준 커밋 2742848c2dea514d67bce845e42cd39db242475a)
범위: 제2부(STEP 0-1-σ, 0-1-π, A-5-π, B-6-π, B-8, C-α)와 부록 A([A-7], [A-8])만. 공리 A~O 본문과 버전은 수정하지 않는다.
채택 시점: 일치도 평가 기준 v2.0 시험(버전 동결)이 끝난 뒤. 시험 중에는 병합하지 않는다.
주의: desktop.html은 저장소의 최신 klaw_v*.md를 자동으로 불러오므로, 이 패치를 병합하면 즉시 운영 프롬프트에 반영된다.
적용 방법: git apply --check --ignore-whitespace <이 파일> 로 먼저 확인한 뒤 git apply --ignore-whitespace <이 파일>
(아래 첫 diff 줄 앞의 설명 텍스트는 git apply가 무시한다.)
------------------------------------------------------------------------

diff --git a/klaw_v15_1.md b/klaw_v15_1.md
index 3b36a28..fedebcd 100644
--- a/klaw_v15_1.md
+++ b/klaw_v15_1.md
@@ -1,4 +1,19 @@
 ﻿# K-Law 판결 방법론 v15.1
+## ══ 평가 프로토콜 정합 패치안 (2026. 9. 24.) — 적용 보류 ══════════
+## 성격: 제2부(절차)와 부록 A(출력 서식)만 갱신 — 공리 A~O 본문과 버전(15.1)은 무수정.
+## 시점: 일치도 평가 기준 v2.0 시험(버전 동결)이 끝난 뒤에 채택한다. 시험 중에는 병합하지 않는다.
+##       (desktop.html이 저장소의 최신 klaw_v*.md를 자동으로 불러오므로 병합 즉시 운영 프롬프트에 반영된다.)
+## 주요 갱신:
+##   1. STEP 0-1-σ ④ 입력 모드(검토/독립) 선언 신설
+##   2. STEP 0-1-π: 독립 모드 대체 출력. A-5-π·B-6-π: 독립 모드에서 원심 정보 미제공만으로 발동하지 않음
+##   3. STEP B-8: 독립 모드 결론 방향 출력 신설. C-α (11) 문언 정합
+##   4. 부록 A [A-7]: '[AI 예측 일치도: XX%]' 삭제. [A-8]: 강제규칙 참조 범위를 1~7 및 9~25로 정정
+## 보류(공리 수정이라 버전 상향이 필요하므로 시험 결과 후 결정):
+##   - 공리 G 4대 정보 (d) 원심 판단 요지의 모드별 적용
+##   - 공리 G 판결 난이도 지수 표의 '예상 일치도' 문구
+##   - 공리 O: 평가 표본과 패치 근거 배치의 분리 원칙
+## ══════════════════════════════════════════════════════════════
+##
 ## ══ v15.2 패치 노트 (2026. 8. 7. — 주피터님 지시, 관할특유 공백 보강) ══
 ## 갱신 유형: 부록 B(개별 판례·법령 변경)만 갱신 — 공리 O 자기 갱신 원칙에 따라
 ##            공리 A~O·STEP 0~C 골격은 무수정, 버전 자체(15.1)도 유지한다.
@@ -1186,6 +1201,16 @@ STEP 0의 최초 단계로서, 다른 모든 하위 절차에 앞서 수행한
 ③ 출력 형식:
    `[심급 확인: X심 | 판결 생성 모드: (상고심/항소심/1심/심급 불명)]`
 
+④ 입력 모드 선언 (평가 프로토콜 정합 패치안):
+   - **[검토 모드]**: 원심 판단 요지를 포함한 4대 정보가 제공된 경우. 기존 절차(0-1-π, A-5-π, B-6-π, B-8)를 그대로 적용한다.
+   - **[독립 모드]**: 원심·하급심의 판단이 제공되지 않고 사실관계와 청구 취지·답변만 제공된 경우. 다음을 적용한다.
+     · 0-1-π: `[원심 판단 구조화: 독립 모드 — 원심 정보 비제공]` 1줄로 대체하며 '불명' 항목 수에 산입하지 않는다.
+     · A-5-π (a)(b), B-6-π (a): 원심 정보 미제공만을 이유로 발동하지 않는다. 그 밖의 발동 조건은 그대로 적용한다.
+     · B-8: 원심 대비 검증 대신 '독립 모드 결론 방향'을 출력한다.
+     · 사건번호가 제공되지 않은 경우 0-0 ① 당사자 정합성은 개요 내부의 당사자 기재 정합성으로 확인한다.
+   출력 형식: `[입력 모드: 검토 / 독립]` (심급 확인 줄에 이어서 기재)
+   모드가 명시되지 않은 입력은 [검토 모드]로 처리한다.
+
 미이행 시: → 강제규칙 1 위반(단계 건너뛰기 금지), 판결문 무효.
 
 **0-1-π: 원심 판단 구조화 추출 모듈 (v14.2 신설) [★]**
@@ -1206,6 +1231,7 @@ STEP 0의 최초 단계로서, 다른 모든 하위 절차에 앞서 수행한
 
 미이행 시: → 강제규칙 1 위반, 판결문 무효.
 불명 항목이 2개 이상인 경우: → STEP-A-5 페일세이프 발동 트리거.
+※ 독립 모드는 0-1-σ ④에 따른다.
 
 **0-α. 직관적 타당성 예비 점검**
 법적 개념·공리·판례를 배제하고 상식과 논리만으로 어느 쪽 주장이 더 납득되는지 기록.
@@ -1422,6 +1448,7 @@ STEP 0-9 결과에 A-2·A-3 결과 반영. 숨은 명제 탐지 및 완전성 
 - (a) 공리 G 결정 회피 방지 장치의 4대 정보 중 2개 이상 미식별
 - (b) 0-1-π 원심 판단 구조화 태그에서 '불명' 항목이 2개 이상
 - (c) STEP-A-4 완전성 검증에서 포괄성·정합성 모두 '보완 필요'
+※ 독립 모드에서는 (a)의 '원심 판단 요지'와 (b)를 원심 정보 미제공만을 이유로 적용하지 않는다(0-1-σ ④).
 
 발동 시 출력:
 ```
@@ -1606,6 +1633,7 @@ B-4-β 직후 수행:
 - (a) 0-1-π 원심 판단 구조화 태그에서 원심 결론 방향이 "불명"으로 기재된 경우
 - (b) 결론의 전제가 되는 선행 법률관계의 성립 여부가 입력 정보에서 확정되지 않은 경우
 - (c) 동일한 사실관계에 대해 합리적으로 상이한 평가가 가능하여 선행 판단이 복수의 결과를 허용하는 경우
+※ 독립 모드에서 (a)는 원심 정보 미제공만을 이유로 적용하지 않는다(0-1-σ ④).
 
 **미발동 선언**: 위 (a)(b)(c) 중 어느 것도 해당하지 않는 경우 `[B-6-π: 발동 조건 미해당]` 출력 후 B-7로 진행.
 
@@ -1678,6 +1706,15 @@ K-Law 결론 방향: (원고 승소 / 원고 패소 / 각하 / 파기환송)
 미이행 시: → 부록 C "-2점 (법리)" 적용
 근거 공리: → 공리 A(명제 완전 추출), 공리 B(역방향 검증), 공리 G(확신도)
 
+**B-8 [독립 모드] (평가 프로토콜 정합 패치안)**
+독립 모드(0-1-σ ④)에서는 위 서식 대신 다음을 출력한다.
+```
+[독립 모드 결론 방향]
+원고 청구 인용 정도: (전부 인용 / 일부 인용 / 기각 / 각하)
+근거 요약: (1~2줄)
+```
+원심 정보가 입력되지 않았으므로 원심 대비 검증과 3단 논법은 적용하지 않는다.
+
 [STEP-B-COMPLETE]
 
 ---
@@ -1710,7 +1747,7 @@ STEP B 또는 STEP C 출력이 완전 실패한 경우(판결문 본문 부재 
 (8) 행정처분 연계성 분석 출력 여부 (해당 시)
 (9) 역방향 판결 초안 생성 및 [비교 분석] 출력 여부 (B-5-A 발동 조건 해당 시만 점검) 【v13.1 신설】
 (10) 국제 비교 법원칙 검증 결과 출력 여부 (B-5-B 발동 조건 해당 시만 점검) 【v13.2 신설】
-(11) B-8 원심 대비 결론 검증 출력 여부 + 반대 결론 시 3단 논법 서술 여부 【v14.2 신설】
+(11) B-8 원심 대비 결론 검증 출력 여부 + 반대 결론 시 3단 논법 서술 여부 (독립 모드는 '독립 모드 결론 방향' 출력 여부) 【v14.2 신설】
 (12) C-α-2 다수당사자 집단 클러스터링 출력 여부 (당사자 3인 이상 & 역할 구별 가능 시만 점검) 【v14.6 신설】
 (13) C-α-7 항소심 공소장변경-직권파기 연계 검증 출력 여부 (항소심 & 공소장변경 해당 시만 점검) 【v14.6 신설】
 
@@ -2089,9 +2126,9 @@ Q1: (피고 반박)
 ### [A-7] 자기 평가 출력 서식
 
 ```
-[AI 예측 일치도: XX%]
 [사건 복잡도: X/10]
 ```
+※ 평가 프로토콜 정합 패치안: 예측 일치도 수치는 출력하지 않는다(일치도 평가 기준 v2.0, 출력형식 v14.0).
 
 ---
 
@@ -2100,7 +2137,7 @@ Q1: (피고 반박)
 ```
 ==============================
 [자기 검증]
-□ 강제규칙 1~7 및 10~16 모두 준수: 예 / 아니오
+□ 강제규칙 1~7 및 9~25 모두 준수: 예 / 아니오
 □ 준수하지 않은 규칙: 없음 / (번호 및 사유 명시)
 □ 독립적 재판단 선언 출력 여부 (2안 이상 시): 출력 / 해당 없음
 □ 재생성 필요 여부: 불필요 / 필요
